server:
  port: 10000

spring:
  application:
    name: gulimall-product
  profiles:
    active: dev  # use application-dev.yml (e.g. RDS) when running locally
  cloud:
    alicloud:
      access-key: ${ALIYUN_ACCESS_KEY_ID:}
      secret-key: ${ALIYUN_ACCESS_KEY_SECRET:}
      oss:
        endpoint: oss-cn-hangzhou.aliyuncs.com
        bucket-name: gulimall-a95569
    consul:
      host: localhost
      port: 8500
      discovery:
        enabled: true
        hostname: host.docker.internal
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/gulimall_pms
    username: postgres
    password: Bb77937794!
  sql:
    init:
      mode: never
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

logging:
  level:
    com.atguigu.gulimall.product: debug